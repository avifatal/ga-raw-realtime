{
  "name": "ga-raw-realtime",
  "version": "1.0.0",
  "description": "Google analytics raw data to google big query in real time",
  "main": "index.js",
  "scripts": {
    "start": "node dist/app.js",
    "start-dev": "set NODE_ENV=development && cp keyfile.json dist/ && cp .env dist/  && nodemon dist/app.js",
    "local": "",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": "10.x"
  },
  "keywords": [
    "Google analytics",
    "google bigquery",
    "realtime",
    "raw data"
  ],
  "author": "Avi Fatal",
  "license": "ISC",
  "dependencies": {
    "@google-cloud/bigquery": "^2.1.0",
    "@google-cloud/pubsub": "^0.30.1",
    "dotenv": "^8.0.0",
    "express": "^4.17.3",
    "googleapis": "^41.0.1",
    "reflect-metadata": "^0.1.13",
    "routing-controllers": "^0.7.7",
    "typescript": "^3.5.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.0"
  }
}
